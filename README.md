```python
readme_content = """# 🏳️ WhitePanel - Cloudflare Worker Proxy Management Panel

یک پنل مدیریتی قدرتمند، سبک و پیشرفته بر بستر **Cloudflare Workers** برای ایجاد، مدیریت و بهینه‌سازی کانفیگ‌های پروکسی (VLESS و Trojan) همراه با پشتیبانی از کلاینت‌های محبوب مانند Clash.

---

## 🚀 قابلیت‌ها (Features)

* **پشتیبانی از پروتکل‌های مدرن:** قابلیت تولید و پردازش کانفیگ‌های امن پروتکل‌های **VLESS** و **Trojan** (کدگذاری شده به صورت پویا به صورت ساختارهای آلفا و بتا).
* **تولید اشتراک کلاش (Clash Subscription):** تبدیل و فرمت‌بندی خودکار تنظیمات شبکه به پروفایل‌های استاندارد فایل YAML جهت استفاده مستقیم در نرم‌افزار Clash.
* **پنل وب داخلی مستقل (Built-in Web UI):** دارای یک رابط کاربری وب مدرن، زیبا و کاملاً واکنش‌گرا (Responsive) با نام **WhitePanel** برای مدیریت آسان متغیرها بدون نیاز به هیچ‌گونه دیتابیس خارجی.
* **سیستم احراز هویت و مدیریت نشست ایمن:** مجهز به ورود رمزنگاری‌شده بر پایه `masterKey` و ذخیره‌سازی توکن‌های منقضی‌شونده در `localStorage` برای امنیت بالاتر مرورگر.
* **مدیریت آی‌پی‌های تمیز (Clean IPs):** امکان تعریف، مانیتورینگ و گردش خودکار آی‌پی‌های سالم کلودفلر جهت بهبود پینگ، پایداری و دور زدن محدودیت‌های شبکه.
* **یکپارچه‌سازی با ربات تلگرام (Telegram Bot Notification):** قابلیت اتصال مستقیم به API تلگرام جهت ارسال هشدارهای وضعیت سیستم، ورودهای ناشناس و گزارش‌های حیاتی به چت یا کانال شخصی شما.
* **سیستم به‌روزرسانی خودکار هوشمند:** بررسی خودکار و لحظه‌ای نسخه مستقر شده با آخرین نسخه موجود در مخزن گیت‌هاب شما و نمایش بنر اطلاع‌رسانی هوشمند به مدیر.
* **پشتیبانی از DNS سفارشی امن (DoH):** ادغام با پروتکل‌های DNS over HTTPS مانند `cloudflare-dns.com` جهت از بین بردن احتمال نشت DNS (DNS Leak) و تسریع در رزولوشن دامنه.

---

## 🛠️ پیش‌نیازها (Prerequisites)

1. یک حساب کاربری فعال در پلتفرم [Cloudflare](https://cloudflare.com).
2. یک دامنه اختصاصی متصل به کلودفلر (جهت دور زدن فیلترینگ آدرس‌های پیش‌فرض `workers.dev` و عملکرد صحیح ساب‌دومین‌ها شدیداً توصیه می‌شود).

---

## 📦 راهنمای نصب و راه‌اندازی (Installation Guide)

### روش اول: نصب سریع از طریق داشبورد وب کلودفلر (توصیه شده)

1. **کپی کردن سورس کد:** ابتدا تمام محتویات فایل اصلی `worker.js` را به طور کامل کپی (`Ctrl+A` و سپس `Ctrl+C`) کنید.
2. **ورود به بخش Workers:** وارد حساب کاربری خود در کلودفلر شوید و از منوی سمت چپ به بخش **Compute -> Workers & Pages** بروید.
3. **ایجاد ورکر جدید:** روی دکمه **Create Application** و سپس **Create Worker** کلیک کنید. یک نام دلخواه برای ورکر خود بنویسید و روی دکمه **Deploy** کلیک کنید.
4. **جایگذاری کد:** پس از اتمام ساخت، روی دکمه **Edit Code** یا **Quick Edit** کلیک کنید تا ادیتور آنلاین کلودفلر برای شما باز شود.
5. **پاکسازی و ذخیره:** کدهای نمونه پیش‌فرض درون ادیتور را کاملاً حذف کنید. سپس سورس کد کپی شده در مرحله اول را پیست کرده و در نهایت از بالای صفحه سمت راست روی دکمه **Save and Deploy** کلیک کنید.

### روش دوم: نصب و توسعه از طریق ابزار Wrangler (محیط خط فرمان)

اگر به عنوان توسعه‌دهنده تمایل دارید پروژه را از طریق ترمینال سیستم خود مدیریت کنید:
1. ابزار فرمان کلودفلر را به صورت سراسری نصب کنید:

```

```text
File README.md successfully generated.

```bash
   npm install -g wrangler

```

2. به حساب کاربری کلودفلر خود متصل شوید:
```bash
wrangler login

```


3. فایل ورکر خود را مستقیماً روی شبکه کلودفلر مستقر (Deploy) کنید:
```bash
wrangler deploy worker.js --name white-panel

```



---

## ⚙️ پیکربندی و متغیرهای پیش‌فرض (Configuration)

در اولین اجرای اسکریپت، مقادیر و تنظیمات سیستم بر اساس آبجکت `SYSTEM_DEFAULTS` در کد تنظیم شده‌اند:

* **رمز عبور پیش‌فرض پنل مدیریت:** `admin`
* **مسیر همگام‌سازی API:** `/sync`
* **پورت پیش‌فرض سوکت:** `443`

### شخصی‌سازی تنظیمات:

برای افزایش امنیت و کارایی، پیشنهاد می‌شود مقادیر پیش‌فرض را در خطوط ابتدایی فایل `worker.js` ویرایش کنید یا پس از اولین ورود، آن‌ها را مستقیماً از طریق فیلدهای تعبیه شده در پنل وب تغییر دهید. مهم‌ترین پارامترها عبارتند از:

* `masterKey`: کلید اصلی ورود به پنل مدیریت وب.
* `cleanIps`: لیست آی‌پی‌های تمیز کلودفلر (با کاما `,` جدا شوند).
* `tgToken` و `tgChatId`: شناسه و توکن ربات تلگرام جهت دریافت نوتیفیکیشن‌ها.

---

## 📱 نحوه استفاده (Usage)

1. آدرس اختصاصی ورکر خود را در مرورگر باز کنید (به عنوان مثال: `https://white-panel.yourdomain.com`).
2. در کادر احراز هویت، رمز عبور خود (در ابتدا `admin`) را وارد کنید تا داشبورد مدیریت **WhitePanel** بارگذاری شود.
3. از طریق بخش مدیریت، کانفیگ‌های بهینه‌سازی شده را دریافت کرده و در نرم‌افزارهای کلاینت خود (نظیر v2rayN, Shadowrocket, Sing-box, v2rayNG یا Clash) کپی و استفاده نمایید.

---

## ⚖️ سلب مسئولیت (Disclaimer)

این پروژه صرفاً با اهداف آموزشی، تحقیقاتی و تست ساختارهای توزیع‌شده شبکه در بستر Cloudflare Edge توسعه یافته است. مسئولیت هرگونه استفاده نادرست، غیرقانونی یا خارج از چهارچوب‌های اخلاقی از این اسکریپت به طور کامل بر عهده شخص کاربر می‌باشد و توسعه‌دهنده هیچ‌گونه مسئولیتی در این قبال ندارد.
"""

with open("README.md", "w", encoding="utf-8") as f:
f.write(readme_content)

print("File README.md successfully generated.")

```
فایل README.md با ساختار استاندارد، ظاهر حرفه‌ای و متناسب با سورس کد شما (WhitePanel) آماده شده است. این فایل شامل بخش‌های معرفی، لیست کامل قابلیت‌های فنی، راهنمای گام‌به‌گام نصب به دو روش (داشبورد و ترمینال)، تنظیمات پیش‌فرض و نحوه استفاده است.

فایل Markdown (README.md) شما آماده است:
[file-tag: code-generated-file-0-1781072480002450666]

### مزیای این فایل برای گیت‌هاب شما:
* **استفاده از ایموجی‌ها و ساختار خوانا:** به کاربران کمک می‌کند بخش‌های مختلف راهنما را به سرعت پیدا کنند.
* **بررسی دقیق قابلیت‌ها:** بر اساس تحلیل متغیرها و کدهای داخل فایل `worker.js` شما (مانند ساختار شناسایی پروتکل‌های VLESS و Trojan، ارتباط با کلاش، ربات تلگرام و بررسی نسخه گیت‌هاب) نوشته شده است.
* **بخش سلب مسئولیت استاندارد:** متنی حقوقی و مناسب برای پروژه‌های متن‌باز حوزه شبکه و پروکسی در آن گنجانده شده است.

شما می‌توانید این فایل را دقیقاً در روت اصلی مخزن (Repository) گیت‌هاب خود آپلود کنید تا به عنوان صفحه اصلی پروژه نمایش داده شود.

```
